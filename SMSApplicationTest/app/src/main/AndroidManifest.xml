<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.activity.login">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:screenOrientation="portrait">
        <activity android:name="com.example.activity.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 主界面 -->
        <activity
            android:name="com.example.activity.login.MainActivity"
            android:screenOrientation="portrait" >
        </activity>

        <!--搜索界面-->
        <activity
            android:name="com.example.activity.search.SearchActivity"
            android:screenOrientation="portrait" >
        </activity>

        <!--课程列表-->
        <activity
            android:name="com.example.activity.courseList.CourseListActivity"
            android:screenOrientation="portrait" >
        </activity>

        <!-- 注册界面 -->
        <activity
            android:name="com.example.activity.login.RegisterActivity"
            android:screenOrientation="portrait" >
        </activity>

        <!-- 注册界面 -->
        <activity
            android:name="com.example.activity.menu.TeacherManegerActivity"
            android:screenOrientation="portrait" >
        </activity>

        <!-- 注册界面 -->
        <activity
            android:name="com.example.activity.login.RegisterAccountActivity"
            android:screenOrientation="portrait" >
        </activity>

        <!-- 注册界面 -->
        <activity
            android:name="com.example.activity.courseList.CourseDetailActivity"
            android:screenOrientation="portrait" >
        </activity>

        <!-- 注册界面 -->
        <activity
            android:name="com.example.activity.courseList.TestActivity"
            android:screenOrientation="portrait" >
        </activity>

        <!-- 注册界面 -->
        <activity
            android:name="com.example.activity.menu.MenuActivity"
            android:screenOrientation="portrait" >
        </activity>

        <!-- 注册界面 -->
        <activity
            android:name="com.example.activity.menu.MyCourseActivity"
            android:screenOrientation="portrait" >
        </activity>

        <!-- 注册界面 -->
        <activity
            android:name="com.example.activity.menu.MyInfoActivity"
            android:screenOrientation="portrait" >
        </activity>

        <!-- 注册界面 -->
        <activity
            android:name="com.example.activity.menu.RePassActivity"
            android:screenOrientation="portrait" >
        </activity>

        <!-- 注册界面 -->
        <activity
            android:name="com.example.activity.courseList.MyCourseDetailActivity">
        </activity>

        <!-- 注册界面 -->
        <activity
            android:name="com.example.activity.Person.PersonModifyActivity">
        </activity>

        <!-- 测试界面 -->
        <activity
            android:name="com.test.TestAndroidActivity"
            android:screenOrientation="portrait" >
        </activity>

        <activity
            android:name="com.mob.tools.MobUIShell"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:windowSoftInputMode="stateHidden|adjustResize"/>
        <!--<provider-->
            <!--android:authorities="com.database.tool.CourseProvider"-->
            <!--android:name="com.database.tool.provider"/>-->
    </application>

</manifest>
